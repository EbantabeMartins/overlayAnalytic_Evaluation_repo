version: 2

models:
    - name: stage_working_capital
      columns:
        - name: daily_balance
          tests:
            - not_null
      columns:
        - name: account
          tests:
            - accepted_values:
                values: ['Account1', 'Account2', 'Account3', 'Account4']